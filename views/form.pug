extends layout 

block content 
    form(method="POST", action="/new")
        h2=title
        label(for="authorInput") Author:
        input(type="text", name="authorInput")
        label(for="messageInput") Message: 
        textarea(name="messageInput") 
        button(type='submit') Submit
