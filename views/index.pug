extends layout

block content
  h1= title
  p BEHOLD: The #{title}
  button(type="button")
    a(href="/new") Write New Message
  ul
    each obj in messages
      li
        each val, key in obj
          p= key + ": " + val 
